# 企业素材库改进建议

> 基于《信息系统安全等级保护测评（等保2.0）项目+金盾检测技术股份有限公司.docx》投标文件分析
> 财务数据维度由用户手动填写，不纳入素材库范围。

---

## 缺陷2：业绩案例评分维度建模不完整

**核心问题：** 案例存进来是"死"的，无法在后续使用时被有效筛选和评估。

**改进建议：**

在业绩案例素材上增加三个结构化字段：

- **行业分类**：`保险` / `银行` / `证券/基金` / `其他金融` / `政务` / `医疗` / `其他`
——直接对应招标文件的加分权重，需机器可读
- **等保级别**：支持多选，标注该合同覆盖 `二级` / `三级` / `四级`
——"同一采购人同时有2+3级"是常见评分前提，必须可机器判断
- **证明文件完整性状态**：从模糊的 `list_only / partial / complete` 改为明确的 checklist
——合同签约页 ☐ / 项目名称页 ☐ / 服务日期页 ☐（招标审���的就是这三要素）

业绩案例列表视图应直接将行业分类和等保级别显示为标签，让用户扫一眼就能判断哪些案例"可用"、哪些"有缺陷"。

---

## 缺陷3：人员维度缺少团队整体统计视角

**核心问题：** 人员 Tab 是"人员档案浏览器"，但使用侧需要的是"团队能力快照"。

**改进建议：**

**人员 Tab 顶部增加统计条：** 固定展示当前库/当前文件来源的人员构成概览
——初级 X 人 / 中级 X 人 / 高级 X 人 / 项目经理 X 人

**项目经理金融行业项目数聚合：** 在项目经理详情页，`domainExperiences` 中金融行业条目应可计数展示（如"金融行业等保项目：7个"），而��仅显示文字描述。此数字直接对应评分项。

**增加"本次投标配置"标记：** 人员应有可标记状态——`已配置进本次投标` / `备选` / `未配置`
——统计条需反映"本次投标团队"的准确数字，而非全库数字

---

## 缺陷4：机构资格类证书类型缺失

**核心问题：** 所有证书混为一类，但在投标中法律性质完全不同，混在一起导致分类混乱、后续筛选困难。

**改进建议：**

将企业资质拆分为三个子类别（UI 上用标签区分）：


| 子类别        | 含义         | 典型例子                         |
| ---------- | ---------- | ---------------------------- |
| **机构资质**   | 主体资格，入围门槛  | CMA、CNAS、等保网备案、营业执照、服务认证证书   |
| **管理体系认证** | 体系能力，评分加分项 | ISO 9001、ISO 27001、ISO 20000 |
| **荣誉资质**   | 品牌形象，商务部分  | 高新技术企业、瞪羚企业、质量标杆             |


三类在使用时角色不同：机构资质是"有没有"的门槛判断，管理体系是"有几项"的得分叠加，荣誉资质是"是否展示"的商务包装。后续智能体做精准 `缺/有/过期` 判断时依赖此分类。

---

## 缺陷5：技术方案类素材完全空白

**核心问题：** 技术方案是每次投标最费时也复用率最高的内容，但当前素材库对此没有设计。技术方案是长文档，与证书/案例/人员的 `keyInfo[]` 结构不兼容。

**改进建议：**

独立设计"方案模板"子类别，包含以下字段：

- **适用项目类型标签**：等保测评 / 密评 / 安全服务 / 系统集成……（决定哪次投标可调用）
- **文档章节结构**：按标书常见技术方案章节预设，如：需求理解 / 实施方案 / 测评工具 / 质控措施 / 保密措施 / 验收方案
- **版本号**：方案会迭代，需记录当前版本
- **最后修改时间**：方案内容有时效性（引用年份标准、技术工具等）

目标：当标书生成智能体需要填写技术方案章节时，素材库能提供"等保测评-v3-2025年"这样的具体模板条目，而非空白文本框。